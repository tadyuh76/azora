<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:AvaloniaAzora.ViewModels.Admin"
        x:Class="AvaloniaAzora.Views.Admin.ClassroomManagementWindow"
        Title="Classroom Management"
        Width="1000" Height="600"
        x:DataType="vm:ClassroomManagementViewModel">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Classroom Management" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="View and manage classrooms" Foreground="#777777"/>
        </StackPanel>

        <!-- Search and Actions -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding SearchText}" Watermark="Search classrooms by name or description..." Margin="0,0,10,0"/>
            <Button Grid.Column="1" Content="Search" Command="{Binding SearchCommand}" Margin="0,0,10,0"/>
            <Button Grid.Column="2" Content="Create New Classroom" Command="{Binding CreateClassroomCommand}"/>
        </Grid>

        <!-- Classroom List -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Classrooms}" 
                  SelectedItem="{Binding SelectedClassroom}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*"/>
                <DataGridTextColumn Header="Teacher" Binding="{Binding TeacherName}" Width="2*"/>
                <DataGridTextColumn Header="Students" Binding="{Binding StudentCount}" Width="Auto"/>
                <DataGridTextColumn Header="Tests" Binding="{Binding TestCount}" Width="Auto"/>
                <DataGridTemplateColumn Header="Actions" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" 
                                        Command="{Binding #dataGrid.DataContext.EditClassroomCommand}"
                                        CommandParameter="{Binding Id}"
                                        Margin="0,0,5,0"/>
                                <Button Content="Students" 
                                        Command="{Binding #dataGrid.DataContext.ViewStudentsCommand}"
                                        CommandParameter="{Binding Id}"
                                        Margin="0,0,5,0"/>
                                <Button Content="Tests" 
                                        Command="{Binding #dataGrid.DataContext.ViewTestsCommand}"
                                        CommandParameter="{Binding Id}"
                                        Margin="0,0,5,0"/>
                                <Button Content="Delete" 
                                        Command="{Binding #dataGrid.DataContext.DeleteClassroomCommand}"
                                        CommandParameter="{Binding Id}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Messages -->
        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
            <TextBlock Text="{Binding SuccessMessage}" Foreground="Green" IsVisible="{Binding SuccessMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
        </StackPanel>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="4" Background="#80000000" IsVisible="{Binding IsLoading}">
            <TextBlock Text="Loading..." Foreground="White" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
